class CartModel{
  String name;
  String measurement;
  int totalAmount;
  int originalPrice;
  int quantity;
  String deliverLocation;
  String phone;

  CartModel(this.name, this.measurement, this.totalAmount, this.originalPrice,this.quantity,
      this.deliverLocation,this.phone);
}